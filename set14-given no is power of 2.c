#include <stdio.h>
#include<math.h>
int main(void) {
          int no,i;
          scanf("%d",&no);
          for(i=0;i<no;i++)
          {
          if(pow(2,i)==no){
          printf("yes");}
          }
	return 0;
}
